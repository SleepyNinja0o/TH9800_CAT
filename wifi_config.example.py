# Copy this file to wifi_config.py (gitignored, never committed) and
# fill in your real network credentials.
SSID = "your-network-name"
PASSWORD = "your-network-password"

# Password for the raw packet relay server (esp32_relay.py) that the
# desktop app connects to for its live display mirror. Optional --
# defaults to "" (no password) if omitted.
RELAY_PASSWORD = "your-relay-password"
